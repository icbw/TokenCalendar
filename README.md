# TokenCalendar

> 本地优先的桌面挂件：把本机各类 AI Agent 的 token 用量统一成「Agent/模型 × 日期」的年度热力图，
> 贴桌面常驻、可钻取。Tauri 2 + Rust + React/TS 实现。

## 功能

- **用量矩阵**：Agent/模型 × 日期的年度与月度热力图，支持日/周/累计粒度与双向钻取。
- **本机采集**：只读扫描六类 Agent 的本地日志/数据库（ZCode、WorkBuddy、Claude Code、
  Codex、CodeBuddy、DSH），聚合结果只保存在本机。
- **数据洞察**：用量趋势、模型分布、异常日与积分月报（手写 SVG，不引图表库）；CodeBuddy / WorkBuddy 的模型与积分
  直接读本机数据，无需导入官网账单。
- **项目与任务分析**：按工作目录聚合的项目维度、等待 / 人工时间成本、按会话列出的任务视图
  （轮数 / 步数 / 工具调用 / 耗时 / 错误与中止、逐轮条形）与项目管理；只存元数据，不存对话正文。
- **桌面形态**：主窗口 + 贴桌面挂件 + 订阅额度悬浮球；主题（取色/透明度/圆角/毛玻璃）
  与窗口几何本地持久化。

## 界面

- **主界面**：Agent/模型 × 日期的用量矩阵，Token / 积分双口径、日 / 周 / 累计三档粒度，底部趋势曲线与矩阵行联动；

  ![主界面](screenshots/01-main-matrix.png)

- **桌面挂件**：年度热力图贴片，贴桌面常驻、可钻取；

  ![桌面挂件](screenshots/02-widget-heatmap.png)

- **订阅额度悬浮球**：展开为周额度环 + 中央 5 小时表盘（额度读数、重置时刻、套餐状态，右侧按钮列），
  收起为贴边竖条（周额度水位）；

  ![悬浮球（展开）](screenshots/03-orb-expanded.png)
  ![悬浮球（收起）](screenshots/04-orb-collapsed.png)

## 使用：Tasks 与 Projects

- **Tasks**（标题栏 Matrix / Insights / Tasks）：每行一个会话，列出轮数、步数（模型调用）、工具调用、
  Wait（等待模型与工具的时间）、错误、子代理与 token；点表头排序，点一行展开逐轮条形（红描边 = 被中断的轮）。
  顶部 Idle gaps 卡片是轮间空档分布，超过离开阈值（默认 30 分钟，可改）的空档不计入 Human 时间。
  范围条支持 `7d / 30d / 90d / All / Custom`；选定某个项目时范围自动切到该项目的首末活动日。
  标签可在开始时间与会话标题之间切换（标题只在本机显示）。
- **项目维度**：Matrix 与 Insights 的维度切到 Project，按工作目录看 token；Project 维下可选
  Wait / Human 两项时间指标（并列展示，不相加）。CodeBuddy 按会话记录的工作目录归属项目，临时对话（playground）归入 Scratch。
- **Projects**（设置 → Projects，或项目列表末尾的 Manage projects…）：重命名、隐藏、把多个目录合并为一个项目、
  打开文件夹；会话很少的临时目录默认折叠进 Scratch（阈值可调、可关）。隐藏与合并只影响项目维视图，
  Agent / Model 维的数字不变；这些设置独立保存，清库重扫不会丢失。

> **升级提示**：从 0.6.2 起，统计口径变更不再清库重扫——首次启动先把旧库备份到数据目录 `backups/`，再就地升级已有记录，
> 列表不会清空。0.5.6〜0.5.9 升级到本版会经历最后一次清库（结构差异），首轮扫描完成前列表为空属正常；
> 项目管理设置保留，需要保留旧数字时请先在「设置 → Data」备份。

## 快速开始

```bash
pnpm install                  # 前端依赖（pnpm）
pnpm dev:full                 # 开发模式（= tauri dev）
pnpm tauri build --no-bundle  # 生产构建 → src-tauri/target/release/tokencalendar.exe
pnpm tauri build              # NSIS 安装包 → src-tauri/target/release/bundle/nsis/
```

透明/毛玻璃等窗口效果以生产构建为准；dev 模式下打开 DevTools 时 WebView2 会强制不透明。
NSIS 出包会为更新包签名（`src-tauri/tauri.conf.json` 的 `plugins.updater`），需要本地
配置签名私钥；只验证构建可直接用上方 `--no-bundle` 那条。

## 版本

- **0.9.1** —— **剩余消息数估算修正**:改用最近 14 天全部消息的平均大小、按各模型官方价格统一估算,并乘以各模型在额度读数上实测的额度系数（原中位数口径把大任务忽略,Astra 满窗高估约 3 倍,模型贵贱顺序也可能颠倒）;价格表经官方核对无误未改。不动数据。
- **0.8.9** —— **Insights 分模型消息数表**:价格面板新增每个模型 5 小时 / 周窗口「约剩 / 满窗」条数、本周已发条数、区间内周重置次数（含提前重置）;周窗口按读数里的实际重置算,不固定 7 天,多账号分别计算。不动数据。
- **0.8.8** —— **分模型剩余消息数**:悬浮球 5 小时表盘悬停显示每个模型「约剩几条 / 满窗约几条」（如 `GPT-6 Astra: ~3/30`,按你近 30 天每条的中位代价估算）;设置里可选 Auto / Off / 指定模型;提示延迟 0.3 秒。不动数据。
- **0.8.7** —— **Codex 读数不倒退 + 按需取数**:同一 5h 窗口内读数只升不降;服务器按调用记账、本地读数落后一次调用,改为只在一轮结束或大调用后久无后续时向服务器取数（实测回放请求 16 → 3）;本地零成本读取不占取数间隔;30 分钟兜底保留。不动数据。
- **0.8.6** —— **Codex 读数跟得更紧**:每采集到一批 Codex token 就零请求读一次会话文件里的服务端额度,悬浮球约每 30 秒更新,不再等攒够取数阈值;请求次数不变。不动数据。
- **0.8.5** —— **订阅回本倍数 + Insights 三模块切换**:设置里填自己那一档的月费,价格面板显示「Value vs. fee ≈ N×」(价值倍数,不是省钱);Insights 顶部 Usage trend / Pricing / Credit 一键滚到对应模块,模块工具栏停靠顶部,Credit 随设置开关出现。不动数据。
- **0.8.4** —— **时间统计迁入 Tasks**:Tasks 空档卡升为 `Idle gaps ⇄ Time spent`,按项目 / 任务 / 日堆叠 Task 与 Human 时间,按每轮发生的本地日计入;热力图与 Insights 移除 Wait / Human,只看 token。不动数据。
- **0.8.3** —— **价目新增三款模型**:Claude Opus 5.5(4/20,单独成行)、GPT-6 Sol、GPT-6 Luna;存量订阅样本按新价目就地重算,不删记录。
- **0.8.2** —— **项目固定配色**:与 Agent 同一套色环,每个项目一个独立颜色,首次出现在图表时分配并保存,此后跨重启 / 时间范围 / 视图不变;Scratch / Hidden / Unknown 为中性灰。只改界面,不动数据。
- **0.8.1** —— token 分项**配色按价格顺序**:Output 最深 → Input → Cache write → Cache read 最浅（最深 = 模型本色）;CodeBuddy 积分图改为四分项堆叠,**By model 时所有模型画在同一张图**(每模型并排柱 + 各自积分曲线)。只改界面,不动数据。
- **0.7.9** —— **总 tokens 拆成 Input / Cache write / Cache read / Output 四个互斥分项**:矩阵与 Insights 新增 Cache W / Cache R 指标;Insights 筛到单个模型时按分项出图(按量从多到少,本色由深到浅,柱高 = 总量)。ZCode 的 Input 改为未命中缓存的输入(总量不变)。首次启动就地升级数据库(先自动备份,不清库)。
- **0.7.8** —— 应用内更新改为**启动时自动检查 + 后台下载 + 系统通知,由你点 Install 才安装**（顶栏 Settings 出现提示点,直达 About）;修复更新时偶尔弹「Error opening file for writing」:程序内只跑一次下载、一个安装器,安装器也只允许单实例,并等程序文件真正可写再覆盖。不动数据。
- **0.7.7** —— Insights → Pricing 的**官方价目表可按列排序**（默认按生效日期从新到旧,未公布的价格始终排在最后）,**价格有变动的格子标箭头**:涨价红色 ↑、降价绿色 ↓,点箭头看变动量;去掉几乎全是平线的「价格梯度」图。只改界面,不动数据。
- **0.7.6** —— 修复 **TokenCalendar 后台读取 CodeBuddy 记录时让 CodeBuddy 提示「无法保存消息」**：CodeBuddy 以「写临时文件再改名覆盖」保存,Windows 上文件被别的程序打开着改名就失败;TokenCalendar 原先边读边解析,每次占着文件约 0.66 秒,现在一次读进内存立刻关闭（约 0.1 毫秒）。只改读取方式,不动数据。
- **0.7.5** —— 左下角采集状态**如实区分「正在采集」与「已经采完」**：原来它读的是上一次运行留下的记录,刚启动、第一轮采集还没跑完就显示 `Collectors 6/6 OK`。现在第一轮跑完前显示 `Collecting… <正在采集的来源>`,之后只有跑得久的一轮才再显示;每轮采完立刻刷新健康状态;暂停时显示 `Collection paused`。只改显示,不动数据。
- **0.7.4** —— 修复 **CodeBuddy 用量从 9 月 20 日起几乎不再更新**：CodeBuddy 改为请求一发出就先写进本机记录（进行中、还没有 token 数）,答完才补用量,而采集进度越过了这些进行中的请求、答完后不再回头读。现在进行中的请求等补上用量再计入,**已经漏掉的用量升级后自动补回**（已计入的不重复）,无清库重扫、无数据库迁移。时间轴在 CodeBuddy 回答中不再误报「答完在等你」。
- **0.7.3** —— 时间轴上 **Codex 与 DeepSeek Harness 的会话显示真正的会话名**,不再只显示开始时刻：Codex 取侧栏线程名（无名退第一条消息首行,改名会跟着更新）,DSH 取它生成的会话标题。历史会话升级后自动补上（源记录还在即可）,用量数字一条不动、无清库重扫。项目列头的「⚠ 几天」标记改到项目名同一行。
- **0.7.2** —— 修复 Windows 上**用 Codex 猛跑时悬浮球的额度数跟不上**：读额度的零请求那一路靠「会话文件有没有被改过」决定要不要重读，而 Codex 正在写的那个文件在 Windows 上**修改时间一直不动**（停在创建时刻,会话结束也不补）⇒ **正在用的那个会话恰恰是唯一看不见的**,全靠 60 秒一次的请求顶着。实测一次 21 分钟用满 5 小时额度：显示最多落后 **8 个百分点**、最陈旧过时 141 秒,而磁盘上躺着 49 条没被读取的读数。改成看「文件有没有变长」（只追加的记录,字节数是准的）,并记住每个文件上次读到多大;顺带修掉「连续跑超过一小时的会话整个文件被跳过」。**无数据库迁移**,已有读数与校准样本一条不动,漏掉的读数只要源文件还在就会补回来。
- **0.7.1** —— 修复 **Claude Code 用量被少算约 100〜220 倍**：Anthropic 的 `input_tokens` 不含缓存，而 Claude Code 的会话记录没有合计字段,程序按「输入+输出」补出来的合计实际上约等于只有输出 ⇒ 合计口径改为**输入 + 输出 + 缓存读 + 缓存写**,六源统一到「含缓存」（只有 Claude Code 一源受影响;悬浮球额度估算与价格面板直接读四项,从未被影响）。升级后 Claude 的历史用量会大幅上升——**不是重复计数,是之前少算了**;数据库**就地升级不清库**,升级前自动备份。新增**价格面板**（洞察页）：等价 API 价值（分模型 + 「1% 额度 ≈ 多少钱」）、价格梯度阶梯图（历史用量永远按它当天的价目算）、官方价目对照表。**它不是账单**——你付的是固定月费,这个数衡量的是等价价值。
- **0.6.9** —— 从本版起**保留额度读数的历史**：此前只留最新一条，看过就丢，「这周每天各用掉多少额度」没有数据可答。现在每条读数都按「窗口 × 时刻 × 来源」留在本地库里，另有一层按天汇总（涨与回收分开记，并记下当天第一笔涨幅是跨多久攒出来的）。**界面上没有任何变化**，这一版是给后面的额度曲线与统计面板打地基。已收割的读数就地补进新表，升级前自动备份，读数与校准样本一条不删。
- **0.6.8** —— 订阅额度的换算系数做准：出厂预设改成「基准档实测 × 你这档的倒数倍率」，
  装上就大致对、不用等样本攒够；同一台机器上轮换账号（含两个同档位账号）不再互相污染校准；
  5h 滚动窗口的「旧用量过期」现在会扣掉，额度用满后的区间不再参与拟合。
  修复全新安装首次启动时 Claude 那一路的校准样本可能建不出来。订阅库就地升级并自动备份。
- **0.6.7** —— 订阅额度取数改由预计消耗驱动：本机 token 按官方 API 价目折成美元当量、乘以自动校准的
  换算系数，达到阈值（设置页 Fetch threshold，默认 5%）才取一次读数，额度见底时自动收紧；boost 开关退役。
  换算系数由本机已有历史一次性重建（Claude 读桌面应用用量历史，Codex 读会话记录里的额度读数），首次启动即准；
  Codex 额度改用会话记录里自带的读数推进显示，零请求、更及时。订阅库就地升级并自动备份，读数与样本一条不删。
- **0.6.6** —— 时间轴等待提醒：已处理过的提醒重启后不再重复亮起；回到 Agent 窗口约 2 秒，该窗口下的等待提醒全部熄灭
  （此前只熄灭最近输入过的那一个会话）；在窗口里看着答完并继续操作约 10 秒后不再亮起。
- **0.6.5** —— 修复未设置固定监测项目组时，时间轴折成顶部细条后只显示一个项目名、点击展开后看板立刻缩回细条。
- **0.6.4** —— 时间轴打磨：双击会话跳到对应 Agent（没开就启动，VS Code 系带项目目录）；格内滚动翻看当天全部会话、
  过去日点击展开；固定监测项目组（设置 → Projects 图钉）；等待提醒自动熄灭；被全屏窗口盖住自动折条；看板外观改版与
  Timeline appearance 设置组。订阅额度在 Claude CLI 凭据过期时回落读取 Claude 桌面应用的本地用量记录。
- **0.6.3** —— 修复 0.6.2 项目列表出现以文件夹编码名显示的重复项目（如 `e--Projects-Foo`，升级时就地改回真实路径）；
  时间轴点击等待中的项目即把对应 Agent 窗口带到前面，Agent 已关闭时灰色保留打开目录入口；修复悬浮球偶发以错误形态启动。
- **0.6.2** —— 修正会话项目归属（Claude Code 对话中 `cd` 进子目录、ZCode 子代理在子目录运行时被拆成子目录伪项目或
  归入 Scratch），改为按各 Agent 自己的会话记录归属；**升级不再清库重扫**：先备份旧库到 `backups/`，再就地升级已有记录。
- **0.6.1** —— 修复 Windows「文本大小」大于 100% 时悬浮球只剩左上角一道弧（悬浮球随文本大小同步放大）；
  修复 Claude Code 续聊 / fork 副本重复计数（轮数与 token 高估约三分之一），**升级会一次性清库重扫**；
  新增第四窗口「时间轴 Timeline」只读预览（项目 × 日看板，托盘或设置 → General 打开）。
- **0.5.9** —— 界面打磨：Matrix 的颜色刻度与排序控件从工具栏移到热力图右缘的图标开关
  （默认全局刻度 / 按 tokens 排序，Model 标签页额外提供模型家族分组），工具栏更窄；
  修复 Insights 图表纵轴顶端刻度被裁切。
- **0.5.8** —— CodeBuddy 对话按真实工作目录拆分项目（不再全部归入 Scratch），任务列表显示会话标题；
  CodeBuddy 模型与 CodeBuddy / WorkBuddy 积分改读本机数据，**移除官网账单导入**；设置 → General 新增采集频率
  （30 秒 / 1 / 2 / 3 / 5 分钟）。**升级会一次性清库重扫**，已导入的官网账单不再使用。
- **0.5.7** —— 项目与任务分析：Matrix / Insights 项目维度与 Wait / Human 时间成本、Tasks 视图（逐轮条形、
  中止与错误分列、离开阈值与空档分布）、范围 All / Custom 与项目生命周期、Projects 管理（重命名 / 隐藏 /
  合并 / Scratch）；修正 Claude Code token 重复计数（约高估 2.7 倍）、Codex 子代理轮误计、ZCode 子会话计轮、
  WorkBuddy 纯文本回复漏计，ZCode 补入小时粒度。**升级会一次性清库重扫**，部分历史数字会变化。
- **0.5.6** —— 修复应用内更新（0.5.2 起「下载完成后不启动安装器」的根因 = 更新签名公钥与私钥不配套）；
  该区间版本需手动下载本版一次，之后即可正常一键更新。
- **0.5.5** —— 订阅监控健壮性修复（待机深档时刷新 / 绑定 / 改间隔立即生效，解绑即停止加速检测请求，
  退出回落不再跳变，开关切换不重复抓取），并补全主界面 / 洞察 / 明细 / 设置页的悬停提示。
- **0.5.4** —— 悬浮球鼠标交互修复（透明画布不再抢占下层窗口的点击与悬停，右键只在球体上响应），
  消除划过容器的原生边框闪烁与跨屏贴边过程中的窗口抖动。
- **0.5.3** —— 悬浮球多显示器修复（跨屏贴边不再嵌入接缝、形态转换不再闪烁、
  收起态竖条吸附更一致），启动时恢复上次形态与位置。
- **0.5.2** —— 悬浮球交互与视觉打磨（展开圆盘 / 贴边竖条 / 悬停浮层 / 刷新动画 /
  多显示器几何重做）、Claude 授权检测修复、新增应用内更新（签名校验 + 自动更新开关）。
- **0.3.7** —— 首个公开版本：主界面 / 桌面挂件 / 订阅额度悬浮球三窗口，
  六源本机采集（ZCode、WorkBuddy、Claude Code、Codex、CodeBuddy、DSH），
  数据洞察（趋势 / 模型分布 / 异常日 / 积分月报），主题化与贴片化工作区吸附。

## 许可

MIT，见 [LICENSE](LICENSE)。第三方组件、署名与许可义务见
[THIRD-PARTY-NOTICES.md](THIRD-PARTY-NOTICES.md)。
